codecov:
  branch: master

coverage:
  status:
    project:
      default:
        target: 81.5
        threshold: '0.5%'
        if_not_found: success
        informational: false
        only_pulls: false
        if_ci_failed: error
    patch:
      default:
        threshold: '0%'
        if_not_found: success
        informational: false
        only_pulls: false
        if_ci_failed: error