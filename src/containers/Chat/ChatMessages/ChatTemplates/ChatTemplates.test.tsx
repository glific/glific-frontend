import React from 'react';

import { shallow } from 'enzyme';
import ChatTemplates from './ChatTemplates';

// Initialization

// Dummy API should return results

// No results should return text

// onClick of text should trigger text to fill message input
